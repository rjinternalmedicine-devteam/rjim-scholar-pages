<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>RJIM Abstracts</title>
  <meta name="description" content="Abstracts for Researchers’ Journal of Internal Medicine (RJIM), with Google Scholar–friendly metadata." />
</head>
<body>
  <h1>Researchers’ Journal of Internal Medicine — Abstracts</h1>
  <p>Browse abstracts. Each page includes Google Scholar citation meta tags and links back to the full article on our main site.</p>

  <ul>
  {% for item in collections.articles | reverse %}
    <li>
      <a href="{{ item.url }}">{{ item.data.title }}</a><br />
      <small>
        {{ (item.data.authors or []) | join(", ") }}
        {% if item.data.pubDate %} — {{ item.data.pubDate }}{% endif %}
      </small>
    </li>
  {% endfor %}
</ul>
</body>
</html>
